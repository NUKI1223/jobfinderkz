# Проверка прототипа — 2026-09-17

## Автоматические проверки

| Проверка | Результат |
|---|---|
| PostgreSQL 18/pgvector, initial migration | Применена |
| Downgrade/upgrade initial migration на jobfinder_test | Успешно |
| pytest на PostgreSQL | 31 passed; 2 предупреждения deprecated TestClient-адаптера |
| TypeScript + Vite production build | Успешно |
| npm audit | 0 vulnerabilities |
| pip-audit установленного окружения | No known vulnerabilities found |
| Chromium: полный путь регистрации → статистики → повторный вход | Passed |
| Chromium: mobile 390×844, без горизонтального переполнения | Passed |
| Chromium: административный SRT → черновик → проверка → публикация | Passed |
| Chromium/Chrome/Edge: MediaRecorder → WebM → FFmpeg → подтверждение текста | Passed во всех 3 браузерах |

Playwright всего: **8 passed** (~1.3 минуты). Добавлены CV без ИИ, отдельные поля, локальный Onest, недоступный источник вакансий и передача выбранных HH-фильтров. Голос использует синтетический микрофон браузера. ИИ/STT/HH-ответы фиксированные; настоящие API, PostgreSQL, сессии, файлы и worker работают. Тестовая база изолирована от основной.

## Что было исправлено по результатам тестов

- Боковое меню не прокручивалось, профиль был недоступен на экране высотой 720px.
- Неоднозначные label/select заменены явными htmlFor/id.
- Гонка между чтением records и jobs оставляла старый вопрос при завершённой оценке. Теперь сначала читается состояние jobs, затем данные, устаревшие ответы refresh отбрасываются.
- После logout лишний refresh оставлял сообщение о необходимости входа на следующей сессии.
- Мобильный текст бренда сжимал логотип.
- WebM MediaRecorder без format.duration: длительность определяется по пакетным timestamps.
- Подтверждённый ответ сохраняется до оценки, а не только после успешного ИИ-запроса.
- Миграция 0001 зафиксирована SQL-снимком и больше не меняется вместе с ORM-моделями.
- Для TXT без таймкодов требуется длительность, чтобы учитывать месячную видеоквоту.

## Реальные интеграции

| Источник | Проверено | Не проверено |
|---|---|---|
| YouTube zibAC8HkGFk | RU automatic, 2736 сегментов | Платное извлечение карточек и экспертная публикация |
| YouTube UYmA6p7UwOo | RU automatic, 2027 сегментов | Платное извлечение карточек и экспертная публикация |
| YouTube GlK6nGzAK8E | Обработана недоступность субтитров | Аудиоскачивание/распознавание с ключом |
| OpenAI | SDK-интерфейсы, схемы, бюджет/checkpoints с фикстурами | Живые text/audio/embedding-вызовы и качество RU/EN |
| HeadHunter | MockTransport: регионы/форматы, 429/404, checkpoints при повторе, обновление без дублей и сохранение избранного; браузерная передача фильтров | Живой API с зарегистрированным приложением/токеном |

Платных запросов во время реализации не выполнялось. Ключи не были настроены.

## Визуальные артефакты

Последние просмотренные изображения 2026-09-17: `frontend/test-results/dashboard-desktop.png`, `frontend/test-results/dashboard-mobile.png`, `frontend/test-results/cv-sections.png`. Это тестовые аккаунты, не данные владельца. Onest загружен локально, распределение полей CV видно; горизонтального переполнения нет. HTML-отчёт Playwright: `frontend/playwright-report/index.html`.

## Ограничения заключения

Успешные тесты подтверждают воспроизводимый программный путь, изоляцию, резервирование и обработку файлов. Они не подтверждают фактическую точность живой ИИ-оценки, доступность всех YouTube-видео, наличие 90 проверенных вопросов или готовность к публичному размещению.

## Gemini — 2026-09-19

Добавлены 8 backend-проверок с HTTP MockTransport: схема и редактирование контактов, checkpoint при смене провайдера, явный OpenAI, 429/ручное возобновление, неопределённые timeout/500/некорректный ответ, параллельный дневной лимит, стоимость и capability flags. Это фикстуры, не проверка качества модели. Frontend TypeScript/Vite и 8 Playwright прошли (ИИ/STT/HH фиксированные).

Живые GET models успешны. 2.5 Flash-Lite дважды вернула 404 (недоступна новым пользователям); рекомендованная Google 3.5 Flash-Lite вернула 429. Успешная генерация не подтверждена. Только синтетический CV, личные данные не передавались. Официальные цены: https://ai.google.dev/gemini-api/docs/pricing, $0.30/$2.50, Free Tier доступен, фактический тариф проекта не определяется по ключу.
- Финальный backend-набор после исправления ставок Gemini: **39 passed** на jobfinder_test (2 прежних deprecation warnings).
