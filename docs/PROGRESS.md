# Прогресс — обновление 2026-09-20

Последний checkpoint: администратор настроен; **5 опубликованных вопросов Python junior RU, 5 черновиков, 1 объединённый дубль и 10 опубликованных материалов**. Подборка из пяти разных вопросов с evidence проверена штатным retrieve. Полные видео ещё не обработаны. Пользовательский запуск пока ограничен отсутствием вакансии: HH_ACCESS_TOKEN не настроен, в основной базе 0 вакансий. Следующий независимый шаг — живая проверка оценки на вымышленных ответах в jobfinder_test; затем HH и полный путь пользователя.

В редакторе есть замечания проверки, относящиеся к вопросу субтитры, ссылки на таймкоды и поддержка дробных секунд. Аудио агентом не прослушивалось: спорные карточки заблокированы needs_context. Новая карточка о декораторах проверена по ясному текстовому обсуждению; без утверждения об аудиопроверке. Подробнее: [KNOWLEDGE_REVIEW.md](KNOWLEDGE_REVIEW.md).

Два живых запроса извлечения: оценки $0.003683 и $0.005136; месячный charged=$0.012248. Последние четыре карточки извлечены редактором из сохранённых субтитров без Gemini: дополнительных расходов нет. Это расчёт приложения, не выписка Google. OpenAI отключён. Последние проверки кода: целевой pytest — 1 passed, frontend build, административный Playwright — 1 passed. На новом наборе отдельно проверены реальные публикация, объединение, поисковый индекс/evidence. Старые сообщения о 429 и отсутствии администратора ниже исторические.

Основной журнал для продолжения: [WORKLOG.md](WORKLOG.md). Запуск и использование: [../README.md](../README.md).

## Окружение

WSL исправлен, пользователь перезагрузил Windows. Docker Engine 27.5.1 работает. Python 3.12, FFmpeg и Deno используются в контейнере. PostgreSQL 18 + pgvector запущен; миграция 0001 применена.

## Реализовано

- Аккаунты, Argon2, cookie/CSRF, профиль, роли, проверка владельцев, удаление личных данных.
- PDF/DOCX/текст CV: распределение по заголовкам RU/EN без ИИ, отдельный нераспознанный текст, редактирование/подтверждение, повторное распределение с историей. Весь CV больше не подставляется в summary.
- Вакансии из HH, фильтры, избранное, ранжирование до 20 вакансий. По уточнению пользователя ручная форма убрана, создание через API разрешено только администратору.
- Локальный Onest с кириллицей, более выразительные заголовки и увеличенные размеры основного текста/полей.
- Черновики документов по подтверждённым фактам, редактирование, копирование, DOCX.
- Семидневные планы из опубликованных вопросов, отметки выполнения.
- Интервью из 5 вопросов, текст/запись до 3 минут, подтверждение расшифровки, оценка по 3 критериям, история/продолжение/статистика.
- Источники YouTube, субтитры/аудио/ручные файлы, сохранённые этапы, извлечение черновиков, материалы, публикация и объединение карточек.
- PostgreSQL FTS + pgvector; версионирование критериев в интервью.
- Worker, advisory locks, checkpoints, атомарный бюджет, остановка неизвестных платных результатов.
- Адаптивный интерфейс. Compose запускает сайт на http://localhost:5173, API на 8000.
- WORKLOG, полный CODE_SNAPSHOT и последовательный CODE_CHANGES по требованию пользователя.

## Реальные внешние проверки

- zibAC8HkGFk: 2736 сегментов автоматических RU-субтитров (~115.7 мин).
- UYmA6p7UwOo: 2027 сегментов автоматических RU-субтитров (~89.5 мин).
- GlK6nGzAK8E: субтитры недоступны; нужен аудиоимпорт/ручной файл.
- Все 3 источника добавлены в основную базу; первые 2 с расшифровками на проверке. Вопросы пока не опубликованы.
- Живые OpenAI/HeadHunter-вызовы не проверены: ключи изначально не настроены. Платных вызовов агент не выполнял.

## Проверки на текущем этапе

- Backend: **31 pytest passed** на отдельной базе jobfinder_test; CV RU/EN/DOCX/история, запрет ручных вакансий, блокировка общего источника, защита старого embedding, HH регионы/форматы/повторы/обновления. Миграция не изменялась.
- Frontend: TypeScript и production build прошли.
- npm audit после Vite 6.4.3/Playwright 1.63.0: 0 уязвимостей.
- Playwright: **8 passed** — полный путь, mobile, CV без ИИ/локальный шрифт/недоступный HH, передача фильтров HH, административный импорт/публикация и голос в Chromium/Chrome/Edge. Микрофон синтетический, STT/ИИ/HH фиксированные.
- Desktop/mobile скриншоты просмотрены; исправлены sidebar overflow, label/select, обновление после фоновой оценки и сообщение после logout/login.
- Python audit после обновления FastAPI/Starlette, pypdf, multipart, pytest, yt-dlp и pip: **No known vulnerabilities found**.
- Закреплены 58 Python-пакетов; Deno 2.9.6 подтверждён в runtime 2026-09-16. Сборка того дня завершена. Обновление 2026-09-17 и результат запуска фиксируются в WORKLOG.

## Что ещё не завершено

Приоритет следующих шагов:
1. Настроить серверные OpenAI/HH ключи и проверить живой подбор вакансий, разбор CV, расходы и качество RU/EN.
2. Из двух сохранённых видео извлечь черновики, сверить эталоны с документацией и начать наполнение; третье видео требует аудио/ручной расшифровки. Цель — 90 проверенных вопросов.
3. Доработать перевод/переформулирование документов с проверкой фактов; сейчас исходные утверждения сохраняются дословно.
4. Проверить пользу семидневного плана и оценки интервью на реальных данных; провести итоговую проверку всего проекта.

- Реальное распознавание третьего видео/голосовых ответов и извлечение карточек с OpenAI.
- Экспертная проверка и начальное наполнение 90 вопросов (сейчас 0 опубликованных в основной базе).
- Проверка качества живых оценок RU/EN, авторских субтитров и нескольких говорящих, HH OAuth-токена.
- В текущих документах ИИ выбирает и упорядочивает факты, а фактические формулировки сохраняются дословно. RU/EN меняет обрамление; полноценное переформулирование/перевод фактов ещё не реализовано.
- План использует релевантные опубликованные карточки; при небольшой базе вопросы повторяются. Для проверки полезности нужно реальное наполнение.
- При неопределённом платном результате требуется ручная сверка у провайдера; автоматическое освобождение резерва намеренно отсутствует.

Полный исходный план остаётся незавершённым до внешних проверок и наполнения базы. Не считать прохождение фикстур подтверждением качества живых ИИ-результатов.
Последнее обновление 2026-09-17 (HH-фильтры/регионы/повторы) установлено локально: сборка Docker завершена, сайт HTTP 200 отдаёт новый bundle, API health ok. 31 pytest и 8 Playwright прошли. OpenAI/HH ключи пока отсутствуют. Данные личных аккаунтов не изменялись. Подробности и продолжение — WORKLOG.

## Обновление 2026-09-19: Gemini

Текстовые задачи переключаются через TEXT_PROVIDER=gemini|openai; выбран Gemini 3.5 Flash-Lite. OpenAI сохранён и проверяется фикстурами; автоматического fallback нет. Gemini-only: интервью текстом, полнотекстовый поиск без embeddings. Дневной лимит запросов и месячный бюджет контролируются приложением. Бесплатный режим — только вымышленные данные; флаг не меняет тариф Google.

Frontend build и 8 Playwright прошли; backend добавляет 8 проверок адаптеров/квот/стоимости/повторов. Реальная авторизация Google успешна, генерация пока отклонена HTTP429. Первый приоритет — доступная квота Gemini и повтор синтетического probe, затем проверка качества схем/ответов. OpenAI/HH по-прежнему требуют собственных ключей для живой проверки.
- Итог backend: **39 pytest passed**; frontend build и **8 Playwright passed**. Живая Gemini-генерация всё ещё требует доступной квоты.
- Обновление установлено локально: API health ok, сайт HTTP200 с новым bundle; Gemini выбран в работающем контейнере. Код и журнал сохранены, снимки обновлены.

## Следующие действия — порядок

1. Владелец проверяет проект/квоту `gemini-3.5-flash-lite` в AI Studio. Не включать биллинг без отдельного решения; не повторять generateContent при текущем 429.
2. После появления квоты выполнить ровно один синтетический `app.gemini_probe` на `jobfinder_test`.
3. Если запрос успешен, проверить синтетические живые сценарии CV, ранжирования, документа, плана и интервью RU/EN; отдельно оценить корректность фактов и фактические расходы.
4. Извлечь черновики из двух сохранённых субтитров, сверить с документацией и вручную публиковать проверенные карточки к цели 90. Для третьего видео определить доступную расшифровку отдельно.
5. После настройки владельцем HH token проверить живой поиск и фильтры.
6. Затем закончить перевод/переформулирование документов и выбрать отдельный путь для голосового STT; Gemini key сам по себе голос сейчас не включает.
7. Завершить локальным полным сценарием и обновить все документы/снимки.

До квоты продолжается только работа, не требующая живых запросов Gemini. Mock-тесты не заменяют проверки качества реальной модели.

### Gemini quota finding — 2026-09-19

A single short synthetic generate call from the app returned HTTP429 `RESOURCE_EXHAUSTED`, exact provider message: prepayment credits are depleted. This is a depleted Google Prepay balance, not evidence that this app quickly consumed a daily free quota. API key model listing succeeds but does not test generation quota. Local `.env` accounting/UI flag GEMINI_FREE_TIER corrected to false; this does not change Google's billing or add funds. Google documents disabling project billing to return to Free Tier; owner should inspect AI Studio Billing and Usage. Avoid payment unless owner decides. No browser account UI available in this session.
- После квотной диагностики локальный флаг Free/Paid скорректирован и применён пересозданием API/worker. Health HTTP200; ключ не выводился; повторных generate запросов после диагностики не было.

User clarified the OPENAI_API_KEY field held the Gemini key. It was sent once in a non-billable OpenAI models-list auth check and returned 401; user informed and advised to rotate Gemini key. OPENAI_API_KEY has now been cleared locally; Gemini API key untouched. Latest safe Gemini check: models-list 200, one synthetic CV generation succeeded with valid schema (test database). Voice remains unavailable without a real OpenAI key.
- OpenAI key value removed from local env and container; Gemini remains active, API healthy. No keys shown. CODE snapshots refreshed.
