# Журнал работы и восстановления контекста — JobFinderKZ

Обновлять после каждого значимого изменения. Это основной файл для продолжения после чистки контекста.

## Актуальная сводка (читать первой)

- **2026-09-19, новый проход:** Gemini реализован, OpenAI сохранён отдельным адаптером; TEXT_PROVIDER=gemini и модель 3.5 Flash-Lite выбраны в локальном .env. Живая генерация пока НЕ подтверждена: Google вернула 429; прежняя 2.5 Flash-Lite недоступна новым пользователям несмотря на список моделей. Смотрите последний раздел этого журнала для реализации, проверок и продолжения. Следующий пункт описывает состояние ДО этих изменений.

- 2026-09-19: пользователь добавил GEMINI_API_KEY в корневой .env. Ключ проверен GET /v1beta/models официального Google API: авторизация принята, gemini-2.5-flash-lite доступен в списке. Генерации и передачи CV не было; квота генерации/Free Tier этим не проверяются. Один и тот же ключ оказался также в OPENAI_API_KEY: ошибочная копия очищена, GEMINI_API_KEY сохранён. Значения не выводились и в документацию не включались. Поддержка Gemini в backend пока НЕ реализована; текущий ai.py работает только с OpenAI. Следующий шаг — адаптер текстовых запросов Gemini, отдельная индикация возможностей, тесты и перезапуск сервисов. Предыдущий проход был прерван до изменений кода.

- Текущий проход 2026-09-17: доработан HH — фильтры/регионы профиля, формат работы, обновление без дублей, checkpoints списка/карточек, сообщение об ошибке. **31 pytest passed, 8 Playwright passed**, frontend build прошёл. Проверка наличия настроек: openai_configured=False, hh_configured=False. Compose обновлён успешно; API/БД healthy, worker/web Up; HTTP 200 сайта отдаёт `index-BnEzVStw.js`, API health ok. Снимок кода обновлён.

- 2026-09-17: изменения по замечаниям пользователя доставлены в локальное приложение — Onest, удаление пользовательской формы вакансий, распределение CV. Compose build/up завершился с кодом 0; API/БД healthy, worker/web работают; сайт HTTP 200 с новым bundle, API health ok.
- Проверки: **28 pytest passed**, **7 Playwright passed** (полный путь, mobile, CV без ИИ/загрузка шрифта/недоступный HH, admin review/publish, запись голоса Chromium/Chrome/Edge). ИИ/STT/HH в браузерных тестах фиксированы.
- npm audit и pip-audit: известных уязвимостей не обнаружено после обновления зависимостей.
- Последняя проверка основной БД 2026-09-16: 0 пользователей, 3 реальных источника, 0 опубликованных вопросов. Эти числа исторические; пользователь мог добавить данные. В текущем проходе личные записи не читались и не изменялись.
- OpenAI и HH ключи не настроены. Платных вызовов не было. Полный исходный план ещё требует живых интеграций и 90 проверенных вопросов; документация содержит ограничения текущей адаптации CV.
- E2E использовал отдельную `jobfinder_test` и остановлен/удалён после тестов. **При наличии одноразового compose-контейнера с label service=api команда `docker compose exec api` может выбрать его! Для точных проверок используйте `docker exec jobfinderkz-api-1 ...`.** Прямое сравнение подтвердило, что основная БД не затронута.
- Следующий этап после текущей проверки запуска: настроенные внешние интеграции, качество живого разбора/оценок, экспертное наполнение. Сначала читать уточнения пользователя от 2026-09-17 ниже. Старый session 41058 уже завершён и не актуален.

## Задача и инструкции пользователя

- Реализовать весь план [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md), а не только предложить архитектуру.
- Локальный веб-прототип: React/TypeScript/Vite, FastAPI/Pydantic/SQLAlchemy/Alembic, PostgreSQL 18 + pgvector, отдельный worker.
- Регистрация → CV → вакансии → документы → 7 дней подготовки → 5 вопросов интервью → статистика.
- Бюджет приложения до $20/месяц, видео до 20 часов/месяц. Нельзя повторно оплачивать неизвестный результат запроса после сбоя.
- Публиковать вопросы только после проверки документации и критериев. Цель 90 вопросов не выполнена; не выдавать сгенерированные или тестовые данные за проверенную базу.
- Пользователь 2026-09-16 дополнительно потребовал фиксировать весь код, изменения и действия в файлах для восстановления контекста.
- Ссылки пользователя для реальной проверки:
  - https://www.youtube.com/watch?v=zibAC8HkGFk
  - https://www.youtube.com/watch?v=UYmA6p7UwOo
  - https://www.youtube.com/watch?v=GlK6nGzAK8E&list=PLYYFv9OgYHjZ66bObZR0S0QpZpG5wWFqu
  - Последний URL нормализуется к отдельному видео; плейлист не импортируется.

## Карта файлов для восстановления

- [CODE_SNAPSHOT.md](CODE_SNAPSHOT.md): полный актуальный код и конфигурация без секретов.
- [CODE_CHANGES.md](CODE_CHANGES.md): последовательные полные версии изменённых файлов с SHA-256.
- [PROGRESS.md](PROGRESS.md): этапы и условия запуска (требует обновления при завершении текущего этапа).
- `scripts/snapshot.mjs`: генератор снимка; запуск `node scripts/snapshot.mjs` из корня.
- `AGENTS.md`: обязательное чтение журнала следующими сессиями.
- `backend/app/db.py`: users, sessions, records (версионируемые JSONB-документы), knowledge/vector, jobs, budgets, usage.
- `backend/app/security.py`: Argon2, cookie-сессии, CSRF и роль admin.
- `backend/app/main.py`: 47 маршрутов API на момент первой проверки.
- `backend/app/ai.py`: резервирование и учёт, checkpoints платных ответов, OpenAI structured outputs, embeddings, transcription.
- `backend/app/ingest.py`: YouTube/subtitles/FFmpeg, PDF/DOCX, allowlisted HTTPS с проверкой IP и закреплённым сокетом против SSRF/rebinding.
- `backend/app/tasks.py`, `worker.py`: обработчики и PostgreSQL advisory locks (задача + владелец).
- `backend/app/retrieval.py`: полнотекстовый + pgvector поиск, фильтры и evidence.
- `backend/tests/`: тесты против отдельной PostgreSQL-базы `jobfinder_test`.
- `frontend/src/main.tsx`: все экраны и запись микрофона; `styles.css`: адаптивное оформление; `api.ts`: клиент API.
- `compose.yaml`: db, migrate, api, worker, web. Порты только loopback: 8000/5173.
- `.env.example`: шаблон настроек. Реальный `.env` не читать в вывод и не включать в снимки.

## Окружение и исправление WSL (выполнено)

- Рабочая папка D:\Projects\jofinderkz изначально пуста, git не инициализирован.
- Windows PowerShell; `rg` отсутствует, поиск через Get-ChildItem/Select-String.
- Node.js 20.20.2 и npm 10.8.2 установлены; Python в PATH отсутствует. Python 3.12 используется в Docker.
- Docker Desktop 4.38.0, Engine 27.5.1, Linux-контейнеры.
- Docker ранее падал с HCS_E_HYPERV_NOT_INSTALLED.
- `scripts/enable-wsl2.ps1` выполнен с UAC: VirtualMachinePlatform уже Enabled; `bcdedit /set hypervisorlaunchtype auto` успешно.
- Пользователь перезагрузил Windows. `docker version` подтвердил доступный Server.
- Docker-команды требуют выхода из песочницы; пользователь одобрил префикс `docker`.
- Проверки CIM подвисали, затем дальнейшая диагностика заменена явной ошибкой пользователя и DISM/UAC. Не повторять старую диагностику: Docker уже работает.

## Выполненные действия — 2026-09-16

1. Сохранён полный функциональный план, начальный прогресс и скрипт WSL.
2. Прочитан навык OpenAI Docs. Проверены официальные страницы structured outputs, speech-to-text, gpt-5.4-mini, а также документация youtube-transcript-api, yt-dlp, pgvector и HH API.
3. Создан Docker Compose. Создан локальный `.env` копированием `.env.example` (не перезаписывать при дальнейшей работе!).
4. Backend-образ успешно собран: Python 3.12, FFmpeg, Deno 2.2.3, зависимости из requirements.txt. Установлены yt-dlp 2026.3.17, youtube-transcript-api 1.2.4, yt-dlp-ejs 0.8.0 (последний ещё нужно закрепить явно). Остальные прямые версии в requirements.txt.
5. Запущен PostgreSQL `pgvector/pgvector:pg18`; Docker-тома `jobfinderkz_postgres`, `jobfinderkz_storage`.
6. Применена миграция `0001` к основной базе `jobfinder`; созданы vector extension, FTS GIN и HNSW-индексы.
7. `from app.main import app` успешно: 47 маршрутов.
8. Реальный бесплатный smoke test `python -m app.probe`:
   - zibAC8HkGFk: youtube-auto, 2736 сегментов, длительность до последнего субтитра 6943.84 с.
   - UYmA6p7UwOo: youtube-auto, 2027 сегментов, 5367.199 с.
   - GlK6nGzAK8E: TranscriptsDisabled; fallback yt-dlp тоже не дал субтитров. Нужно аудио/STT или ручная расшифровка.
   - Сырые результаты первых двух хранятся в закрытом Docker-томе `/storage/probes/<video>/transcript.json`. В логи полные субтитры не выводились.
   - Платных вызовов не было; отсутствие ключа намеренно запрещало аудиораспознавание в probe.
9. Написаны backend и интерфейс основных сценариев. Пока UI ещё не прошёл сборку/браузерную проверку.
10. Создана отдельная база `jobfinder_test`, применена миграция.
11. `pytest -q -p no:cacheprovider`: **13 passed** за 1.45 с. Один DeprecationWarning Starlette/AnyIO.
12. npm install внутри песочницы не имел доступа к registry (EACCES), повторён с разрешением и завершён: 73 packages.
13. `npm audit`: 3 high — Vite 6.3.5 и Playwright 1.52.0. Исправления доступны в Vite 6.4.3 и Playwright 1.63.0. Следующий шаг: обновить, зафиксировать lockfile и повторить audit.
14. Локальный `npm run build` остановился на EPERM записи `tsconfig.tsbuildinfo` (sandbox). Требуется повтор с разрешением либо сборка в Docker. TypeScript-ошибок до этой ошибки не выведено, но сборка ещё НЕ подтверждена.
15. Ранее зависший npm-процесс (session 36464) остановлен Ctrl+C, exit 1. Успешная установка была отдельным запуском.
16. Добавлены AGENTS.md, этот журнал и генератор полных снимков по требованию пользователя.

## Что проверяют 13 текущих тестов

- Полный API-путь с фиксированной моделью: регистрация admin, публикация документации/5 вопросов, CV, ranking, документ, DOCX, план, интервью, статистика и повторный вход.
- Изоляция владельцев, admin-роль, CSRF/Origin, удаление аккаунта.
- Запрет публикации непроверенной карточки и интервью без 5 опубликованных вопросов.
- Снимки эталонов/критериев внутри интервью, исключение ненадёжной оценки из статистики.
- Одновременное резервирование бюджета: только один запрос проходит при нехватке остатка.
- Повторное использование платного checkpoint и запрет повтора после неизвестного результата.
- Нормализация YouTube ID, rolling subtitles, дедупликация источника и восстановление после сохранения транскрипта.
- Запрет URL вне allowlist и частного IP после DNS.
- Приостановка без OpenAI-ключа без списаний; длительность ответа до 3 минут.
- Удаление аккаунта блокируется, пока worker держит lock владельца.

## Важные ограничения / задачи проверки

- OPENAI_API_KEY и HH_ACCESS_TOKEN изначально пусты. Пользователю сказано добавить OpenAI-ключ в `.env` лично, не в чат. Проверять только наличие, не выводить значения.
- Полная реальная обработка видео, аудио/STT, ИИ-карточки, эталоны и 90 проверенных вопросов НЕ выполнены. Не подменять тестовыми фикстурами.
- Документы сейчас собираются из выбранных подтверждённых фактов без добавления ИИ-утверждений. RU/EN меняет обрамление; сами факты пока остаются на исходном языке. Это осознанное ограничение, которое нужно устранить или явно описать.
- План формируется из релевантных опубликованных вопросов, может повторять их при маленькой базе. Нужны проверка качества и наполнение.
- Резерв видео добавлен для аудио и таймированных субтитров. Ручной TXT без таймкодов пока не имеет надёжной длительности — требуется доработка учёта/административного ввода длительности.
- Код аудио использует непрерывные 10-минутные части без перекрытия и с отдельными speaker IDs; надо проверить границы и желательно сделать контролируемое перекрытие.
- Нужны дополнительные проверки SSRF redirect, очереди с 2 worker, загрузки CV/аудио, JSON-схем OpenAI, HH filters, удаления пользовательских файлов.
- Сборка фронтенда, Playwright, визуальный просмотр интерфейса и запуск всего compose ещё впереди.
- В тестах БД очищается TRUNCATE с жёсткой проверкой имени `jobfinder_test`. Никогда не запускать на основной БД.
- При инициализации admin роль получает зарегистрировавшийся email из ADMIN_EMAIL (default owner@example.com). Это локальный прототип; перед использованием настроить email владельца, не создавать фиктивный аккаунт от его имени.

## Точный следующий шаг

1. Обновить Vite/Playwright, собрать frontend, устранить ошибки.
2. Добавить Playwright и пройти UI desktop/mobile; проверить микрофонный fallback.
3. Исправить найденные недочёты (TXT duration, source timing, queue recovery, privacy/deletion); расширить тесты там, где это проверяет реальные риски.
4. Запустить весь compose и подтвердить health + сайт http://localhost:5173.
5. Написать README/инструкции, обновить PROGRESS/WORKLOG, выполнить snapshot.
6. Реальные ИИ/HH проверки проводить только при наличии настроенных ключей; явно зафиксировать всё, что ещё требует ключа или ручной экспертной проверки.

## Продолжение после требования журналирования

- Выполнен `node scripts/snapshot.mjs`: первый снимок 39 файлов, 39 добавлений в CODE_CHANGES.
- Выполнен `npm install --save-dev --save-exact vite@6.4.3 @playwright/test@1.63.0`: успешен, audit **0 vulnerabilities**.
- `npm run build` с разрешением вне песочницы: успешная проверка TypeScript и сборка Vite; JS 259.24 kB (gzip 78.35 kB), CSS 21.94 kB.
- `docker compose up -d --build api worker web`: все сервисы собраны и запущены, миграция завершена, PostgreSQL healthy. Контроль HTTP и браузер ещё предстоят.
- Добавлен изолированный `backend/tests/e2e_server.py`: настоящие API/DB/worker, фиксированные ИИ-ответы, жёсткое требование базы jobfinder_test, тестовая база очищается при старте. Основная база не затрагивается.
- E2E-контейнер `jobfinderkz-e2e` запущен отдельно, 127.0.0.1:8001, APP_ORIGIN=http://localhost:5174. Не запускать pytest одновременно с ним, так как тестовая база общая.
- Добавлены Playwright config и два сценария: полный пользовательский путь/отказ микрофона/продолжение/статистика и mobile layout.
- Запущена установка Chromium через `npx playwright install chromium` (session 91363, проверить завершение).
- Добавлено перекрытие аудиочастей 2 секунды; speaker IDs сохраняют префикс части.
- Для ручного TXT без таймкодов добавлен duration_seconds (0–10800); импорт требует длительность и резервирует видеоквоту. Повторное добавление того же источника может дополнить неизвестную длительность.
- Исправление таймкода карточки обновляет единственную исходную ссылку. При merge все исходные обсуждения сохраняются.
- yt-dlp-ejs 0.8.0 закреплён явно; `*.tsbuildinfo` исключён из git.
- Проверены реальные названия видео через YouTube oEmbed: первые два — Python Junior; третье — Python-собеседование (уровень требует проверки). Добавлен `app/import_probes.py` для переноса только реальных расшифровок в базу без аккаунтов/платных задач.
- Написан README с запуском, .env, admin, очередью, источниками, расходами и отдельными тестами.
- Chromium/FFmpeg Playwright успешно установлены.
- Первый Playwright: mobile прошёл, полный сценарий остановился на реальном layout-дефекте — кнопка профиля в sidebar оказалась ниже viewport 720px и не могла прокрутиться. Добавлена вертикальная прокрутка sidebar и скрытие декоративной подсказки при высоте до 850px. Требуется повтор.
- Реальные источники перенесены `app.import_probes`: первые два review с расшифровками, третий draft для аудио/ручного файла. В основной базе нет фиктивных аккаунтов и вопросов.
- Второй Playwright: mobile снова прошёл; полный сценарий выявил неоднозначную связь label/select (label оборачивал список options). Компонент Field переведён на явные htmlFor/id через React.useId. Повторяем.
- Первый запуск pip-audit использовал неподходящий флаг --disable-pip; завершился без аудита. Запущен повтор с --progress-spinner off в одноразовом контейнере (session 77368).
- pip-audit завершён: 126 записей известных уязвимостей (включая дубликаты алиасов) в 6 пакетах: pip, pypdf, pytest, python-multipart, starlette, yt-dlp. Проверены текущие версии через PyPI API; requirements обновлён до FastAPI 0.141.1, Starlette 1.6.0, pypdf 6.19.0, python-multipart 0.0.32, pytest 9.1.1, yt-dlp 2026.8.19; Dockerfile обновляет pip до 26.2.1. Требуются сборка, совместимость и повтор аудита.
- PROGRESS.md заменён актуальным состоянием: WSL исправлен; приложение реализовано/запущено, проверки ещё продолжаются; реальные ИИ/HH и 90 вопросов не готовы.
- Третий Playwright дошёл до четвёртого ответа и выявил гонку обновления: frontend читал records перед jobs; завершившаяся между запросами задача могла отображаться completed вместе со старым интервью, поэтому дальнейший polling не обновлял данные. Исправлено: сначала читаются jobs, затем records; защита от устаревших параллельных refresh через epoch; side effects вынесены из setState updater.
- При смене аккаунта очищается локальный кэш пользовательских записей. Добавлена остановка медиапотока, если разрешение на микрофон приходит после ухода со страницы.
- Четвёртый Playwright: **2 passed**, полный пользовательский путь 29.1 с, mobile 544 мс. Исправление гонки подтверждено.
- Backend с новыми зависимостями успешно собран. До повторного pytest надо остановить E2E-контейнер.
- Добавлен голосовой E2E (настоящие MediaRecorder/FFmpeg, фиксированная расшифровка), а также fallback длительности через timestamps пакетов для WebM без metadata duration.
- На машине найдены Chrome и Edge; добавлены отдельные проекты для голосового сценария (голосовой тест ещё нужно вынести в voice.spec.ts).
- Подтверждённый текст ответа теперь сохраняется в интервью ещё до оценки, чтобы не потеряться при паузе/перезагрузке. Форма восстанавливает сохранённый ответ.
- Голосовой тест оставлен в journey.spec.ts; проекты Chrome/Edge выбирают его через grep /record audio/, перенос файла не нужен.
- Добавлены 6 backend-тестов: DOCX/таблицы/исходник/удаление файлов, скан и битый PDF, два worker на одной задаче, TXT duration/quota, offsets/speakers/overlap, запрет несуществующего fact ID.
- E2E-контейнер остановлен и удалён перед pytest. Первый прогон после добавления tests/__init__.py упал на imports `from conftest`; исправлено на `from tests.conftest`. Это ошибка сборки тестов, не проверка продукта. Повторяем.
- Расширенный pytest на обновлённых зависимостях: **19 passed**, 2 предупреждения о deprecated адаптере Starlette TestClient/httpx/AnyIO, 2.21 с. Рабочие API используют httpx 0.28.1; замена на httpx2 не требуется для поведения приложения.
- Повторный Python audit: **No known vulnerabilities found**.
- E2E с обновлённым сервером: **5 passed** за ~1 минуту: полный путь, mobile, реальная браузерная запись с синтетическим микрофоном в Chromium/Chrome/Edge. Расшифровка/оценка фиксированные; живой STT этим не проверяется.
- Визуально просмотрены desktop/mobile PNG. Найдены и исправлены: старое сообщение «Войдите в аккаунт» после logout/login (refresh после выхода теперь пропускается), сжатый мобильный логотип (скрывается весь текстовый блок бренда).
- Начальная миграция заморожена в 0001_schema.sql: больше не зависит от будущих изменений Base.metadata. Требуется проверить downgrade/upgrade только тестовой БД. Snapshot теперь включает SQL и README.
- Добавлен административный E2E: ручной SRT → извлечение черновика с неверным ответом кандидата → отказ публикации неполной карточки → заполнение эталона/критериев/материалов/ролей → публикация и ссылка на таймкод.
- Downgrade до base, upgrade до head и повтор pytest на jobfinder_test успешно: **19 passed**. Основная база не изменялась.
- Повтор probe на yt-dlp 2026.8.19 дал те же результаты (две автоматические расшифровки, третья недоступна). Платных вызовов нет.
- Проверена официальная инструкция EJS: https://github.com/yt-dlp/yt-dlp/wiki/EJS — Deno должен быть >=2.3.0. Старый 2.2.3 заменён на 2.9.6 (актуальный tag v2.9.6 получен из официального GitHub API). Предстоит пересборка/контроль runtime.
- Для воспроизводимости добавляется полный Python constraints lock из успешно протестированного образа; Dockerfile устанавливает прямые зависимости с этим lock.
- requirements.lock.txt сохранён: 58 пакетов. Полный compose build запущен с Deno 2.9.6; Docker Hub подтвердил существование тега.
- Финальный браузерный набор: **6 passed** за ~1.2 минуты, включая admin workflow. Desktop/mobile скриншоты просмотрены повторно: сообщение после logout/login и мобильный логотип исправлены.
- Контроль `compose exec api` неожиданно показал тестовые данные — установлено, что команда выбрала одноразовый E2E-контейнер того же service. По точным именам `jobfinderkz-api-1` и `jobfinderkz-e2e` проверены engine.url.database и counts: main jobfinder 0 users/3 sources/0 questions; test jobfinder_test 6 тестовых пользователей/6 вопросов. Основная база в порядке. E2E-контейнер остановлен и удалён.
- Добавлен VERIFICATION.md с точным разделением автоматических, визуальных и реальных внешних проверок. PROGRESS обновлён до 19 pytest / 6 Playwright и чистых аудитов; финальная сборка ещё выполняется.
- Исправлено ожидание ключа/бюджета: состояние paused больше не расходует лимит неудачных запусков, иначе пользователь мог исчерпать все попытки до настройки ключа. Существующий тест no_key расширен проверкой 3 последовательных возобновлений без расходов.

## Команды, которые уже сработали

## Работа 2026-09-17 — исправления и уточнения пользователя

### Продолжение: подбор вакансий и перезапуск окружения

- Пользователь попросил продолжить и перечислить оставшееся. Выбрана доработка HH как следующего шага после удаления ручных вакансий.
- Сверена официальная документация https://api.hh.ru/openapi/en/redoc и https://github.com/hhru/api/blob/master/docs/areas.md. Используются справочник регионов и `work_format` (REMOTE/ON_SITE/HYBRID); числовые ID регионов не зашиты в новый UI.
- Раньше frontend всегда посылал area=40 и игнорировал фильтры, backend фильтровал только legacy remote. Теперь передаются направление/уровень/формат и регион из фильтров либо профиля. Полные названия регионов разрешаются через /areas; неизвестные и неоднозначные названия вызывают понятную ошибку, не расширяют поиск молча. Legacy explicit area сохранён для совместимости API.
- Несколько форматов вакансии сохраняются в work_formats, старое поле сохранено. Фильтр региона также ищет по родительской стране/области. Legacy fullDay не выдаётся за офисный формат.
- Сохраняются этапы справочника, списка и отдельных карточек HH. 404/410 пропускаются; 429/5xx не превращаются в ложный успешный импорт. При повторе уже полученные карточки не загружаются заново. Существующая вакансия обновляется без дубля, избранное сохраняется, оценка при изменении требований сбрасывается.
- Добавлены tests/test_hh.py (MockTransport, не живой HH) и браузерный тест передачи выбранных фильтров. Frontend build успешен, **31 pytest passed**, 2 прежних предупреждения TestClient.
- Первый backend-запуск остановился из-за выключенного Docker Engine. Docker Desktop запущен скрыто. Обнаружено: API/worker/web восстанавливаются, а db не имеет restart policy. База запущена `docker compose up -d db`; добавлен `restart: unless-stopped` для PostgreSQL. Тома не удалялись.
- Тестовый E2E-контейнер использовал только jobfinder_test: **8 passed** за ~1.3 минуты. Контейнер остановлен и удалён. Наличие ключей проверено исключительно булевыми признаками: оба отсутствуют. Платных запросов не выполнялось. Запущен Compose build/up с обновлённым restart policy db и приложением.

### Предыдущий проход: интерфейс и CV

- Прочитаны AGENTS, план, прогресс, журнал и исходники API/worker/AI/tasks/retrieval/схем/интерфейса/тестов. Это целевая проверка текущих дефектов, не заключение о полном независимом аудите всего проекта.
- При проверке дедупликации уточнено: частичный уникальный индекс `shared_dedup` уже присутствует в миграции 0001, хотя отсутствует в ORM `__table_args__`. Предварительное предположение о допускаемых базой дублях не подтвердилось; новую миграцию не добавляли.
- Worker теперь блокирует общий source_id для import/extract независимо от владельца. Второй уже поставленный импорт использует готовую расшифровку, не повторяет сеть/расходы. Временные каталоги разделены по source/job; собственный checkpoint импорта по-прежнему позволяет закончить очистку после сбоя.
- Извлечение вопросов сохраняет входную расшифровку/метаданные в checkpoint. Индексация сохраняет текст и версию; старый embedding не записывается в новую редакцию, старые неподтверждаемые checkpoints отправляются на проверку. Повторная публикация неизменённого материала не удаляет готовый вектор. Edit/publish/index используют блокировку записи.
- Пользователь уточнил: шрифт невыразителен; пользователь не должен добавлять вакансии; весь CV попадает в «О себе». Эти уточнения добавлены в IMPLEMENTATION_PLAN и имеют приоритет над исходной ручной формой вакансий.
- Корневая причина CV: frontend fallback `summary: row.data.text` до ИИ-разбора. Fallback удалён. Новый `cv_sections.py` распределяет текст по явным RU/EN-заголовкам на загрузке/вставке, оставляет нераспознанное отдельно и не придумывает факты. Старые записи без facts получают такой же предпросмотр. Без заголовков поля остаются пустыми, исходник доступен; для сложной структуры требуется ИИ или ручная проверка.
- Добавлен `POST /cv/{id}/sections` и кнопка повторного распределения. Прежние сохранённые факты добавляются в историю, новая редакция требует подтверждения. Без настроенного ИИ кнопка платного разбора недоступна с пояснением.
- Удалена ручная форма вакансий и прежние подсказки. GET-данные/избранное/фильтры сохраняются. `POST /vacancies` теперь только admin; обычный пользователь получает 403. Пользователь запускает обновление из HH; при отсутствии токена показано состояние недоступности без предложения вводить вакансии.
- Установлен exact `@fontsource-variable/onest`, шрифт с кириллицей поставляется локально. Добавлен typography.css: выразительные заголовки, более читаемые поля/кнопки/текст, сохранена адаптивность. Первый build выявил неправильные имена CSS-подмножеств пакета; исправлено на его реальный index.css import. Последующий build успешен.
- Временный локальный script для удаления длинной JSX-формы выполнен и удалён; окончательный код хранится в main.tsx. README/PROGRESS/VERIFICATION/план обновлены.
- 27 pytest прошли после основных изменений; затем добавлена проверка реального DOCX и истории повторного разбора: **28 passed**, 2 прежних предупреждения TestClient. Tests используют только jobfinder_test.
- E2E переведён с ручной формы на фиксированный HH handler (не live HH); добавлены CV без ИИ, проверка локального шрифта, недоступного HH и отсутствия кнопки ручного добавления. **7 passed** за ~1.3 минуты, включая Chromium/Chrome/Edge audio. Скриншоты dashboard-desktop, dashboard-mobile, cv-sections просмотрены; переполнения по ширине нет, поля CV разделены.
- npm install и npm ci в Docker сообщили **0 vulnerabilities**. Python зависимости не менялись, повторный pip-audit не запускался.
- Тестовый jobfinderkz-e2e остановлен и удалён до финального pytest. Основные пользовательские данные не читались и не менялись. Платных вызовов не выполнялось. `docker compose up -d --build api worker web` завершился с кодом 0. HTTP 200 сайта подтверждает новый bundle `index-DEyaJWkT.js`; API health ok; API/БД healthy, worker/web Up. Снимки кода обновлены после фиксации результатов.

### Ранее проверенные команды

```powershell
docker version
docker compose build api
docker compose up -d db
docker compose run --rm -v D:/Projects/jofinderkz/backend:/app migrate
docker compose run --rm --no-deps -v D:/Projects/jofinderkz/backend:/app api python -m app.probe
docker compose run --rm --no-deps -e DATABASE_URL=postgresql+psycopg://jobfinder:local-jobfinder-password@db/jobfinder_test -v D:/Projects/jofinderkz/backend:/app api alembic upgrade head
docker compose run --rm --no-deps -e DATABASE_URL=postgresql+psycopg://jobfinder:local-jobfinder-password@db/jobfinder_test -v D:/Projects/jofinderkz/backend:/app api pytest -q -p no:cacheprovider
```

Значение local-jobfinder-password здесь — несекретный пароль локальной базы из шаблона, не пользовательский пароль. При изменении .env адаптировать тестовые команды, не выводя реальные секреты.

## Передача Astra — 2026-09-16

- После пересборки Docker Compose все основные контейнеры healthy: API, worker, web и PostgreSQL. API health возвращает `{'status': 'ok'}`.
- Проверен runtime: контейнер API использует Deno 2.9.6, совместимый с yt-dlp EJS.
- Основная база сохранена без тестовых пользовательских данных: 0 users, 3 источника, 0 опубликованных вопросов. Два источника имеют автоматические субтитры и статус review; третий требует аудио или ручного файла.
- Чистый downgrade/upgrade миграции на `jobfinder_test` выполнен; полный backend-набор: **19 passed**, 2 предупреждения о deprecated адаптере TestClient/httpx.
- Frontend production build: TypeScript и Vite build успешно завершены. Ранее зафиксированный npm audit был чистым; повторный сетевой запрос audit сегодня отклонён npm registry, это ограничение сети, а не ошибка сборки.
- Для следующей проверки Astra: прочитать `AGENTS.md`, затем этот WORKLOG, `docs/PROGRESS.md`, `docs/VERIFICATION.md`; проверить `docker compose ps`, `/api/v1/health`, `npm.cmd run build`, pytest на `jobfinder_test` и 6 Playwright сценариев при необходимости. Не выполнять `docker compose down -v` на основной базе.

## 2026-09-19 — Gemini (актуальнее предыдущих сводок)

- Реализованы отдельные адаптеры Gemini/OpenAI и явное переключение TEXT_PROVIDER. Общие Pydantic-схемы, редактирование контактов, checkpoints, резервирование и запрет повторов неизвестного результата сохранены.
- Добавлены безопасные ошибки отказов, возобновление после 429, атомарный дневной лимит Gemini, учёт Free/Paid. Флаги API/UI разделяют текст, голос и embeddings; микрофон недоступен без OpenAI. UI предупреждает о тестовых данных для Gemini Free.
- В корневом .env выбраны Gemini и 3.5 Flash-Lite, ключ не выводился. Адаптер OpenAI сохранён и тестируется. Использован временный codemod Gemini UI, затем удалён.
- Docker был остановлен, запущен скрыто; db запущена. Frontend TypeScript/Vite build прошёл. Первый pytest: 39 passed. После обновления модели повтор выявил оставшиеся старые default ставки (.10/.40); исправлены на .30/2.50, требуется финальный повтор.
- Живые проверки: GET models HTTP200; два синтетических generateContent на 2.5 Flash-Lite вернули 404 (Google закрыла модель новым пользователям). Перешли на рекомендованную 3.5 Flash-Lite после проверки официальных цен; один синтетический вызов вернул 429. Успешной живой генерации нет, личные CV/ответы не отправлялись. Probe удаляет свой синтетический аккаунт, usage остаётся в тестовой БД до следующих фикстур.
- Добавлены test_gemini.py (8 проверок), app/gemini_probe.py; конфиги и README обновлены. E2E и локальная пересборка запущены; результаты будут ниже.
- Следующий шаг: проверить квоту проекта Google AI Studio, повторить probe после восстановления, проверить живые схемы извлечения вопросов/оценки. Затем прежние задачи HH и проверенное наполнение базы. Не считать фикстуры проверкой качества Gemini.
- Финальный backend после исправления ставок: **39 passed**, 2 прежних deprecation warnings. Playwright: **8 passed**, включая Chrome/Edge; фиксированные внешние ответы. E2E остановлен/удалён. Основная БД не использовалась тестовыми фикстурами.
- git status недоступен: каталог не является Git-репозиторием. Изменения сохраняются исходниками и штатными CODE_SNAPSHOT/CODE_CHANGES.
- Локальная установка завершена успешно: compose build/up exit0; точный контейнер jobfinderkz-api-1 подтвердил Gemini 3.5 Flash-Lite, configured=True, OpenAI=False, Free=True, ставки .30/2.50. API health ok, web HTTP200 с новым index-BAx0txNt.js. Секреты не выводились. Снимок: 58 файлов, 13 изменённых полных версий сохранены.

## Очередь дальнейших действий — актуально на 2026-09-19

1. **Разблокировать Gemini без несанкционированных расходов.** Владелец проверяет в Google AI Studio, к какому проекту привязан ключ, квоту для `gemini-3.5-flash-lite`, остаток/время сброса и доступность Free Tier для проекта. Не включать биллинг и не переключаться на платные модели без отдельного решения. До проверки не повторять запросы: последняя живая генерация дала HTTP 429.
2. **Один безопасный живой smoke test после подтверждения квоты.** Запустить `python -m app.gemini_probe` строго на `jobfinder_test`; вход вымышленный. Успехом считать только валидный CVFacts и пустые отсутствующие разделы. Если снова 429 — записать время/код, остановиться и не маскировать проблему.
3. **Проверить реальные текстовые сценарии Gemini на синтетических данных.** CV, ранжирование вакансий, сопроводительное письмо, план, оценка ответов RU/EN. Проверить факты/ссылки, частичные и неверные ответы, остановку при нехватке evidence, логи расходов и отсутствие персональных данных в запросе. Пользовательские CV использовать только после отдельного осознанного решения с учётом условий Google Free Tier.
4. **Наполнить проверенную базу знаний.** Обработать сохранённые субтитры двух видео, получить черновики вопросов и сверить каждую карточку, эталон и критерии с документацией. Не публиковать автоматически. Третье видео отдельно: субтитров нет; вернуться к нему, когда определён доступный способ расшифровки без неожиданной оплаты. Цель плана — 90 проверенных карточек, не синтетические публикации.
5. **Подключить HeadHunter.** Когда владелец настроит официальный токен/данные приложения, проверить регионы, направления, уровни, форматы, обновление без дублей и ранжирование. До этого вакансии из HH и живой подбор остаются неподтверждёнными.
6. **Закрыть оставшиеся продуктовые пробелы.** Перевод/переформулирование документов с сохранением подтверждённых фактов; голосовой STT (сейчас отдельная возможность OpenAI, не следует считать включённой одним Gemini API key); статистика качества интервью на проверенной базе.
7. **Передать результат владельцу.** Полный локальный сценарий на тестовом аккаунте, проверка лимитов/резервирования и восстановления, чистое состояние сервисов, актуальные README/PROGRESS/WORKLOG/CODE snapshots. Публичный запуск вне текущего локального разрешения.

Сначала выполняется пункт 1. До появления доступной квоты пункты 3 с живым Gemini не блокируют работу над проверкой расшифровок и ручной подготовкой базы знаний. Mock-тесты подтверждают обработку интеграции, но не качество живой модели.

## Проверка квоты Gemini — 2026-09-19

- Браузер AI Studio недоступен в CUA этой сессии (браузеры не предоставлены), поэтому выполнен один короткий прямой синтетический запрос из локального API контейнера. Ключ и тело не выводились. Результат: HTTP 429, `RESOURCE_EXHAUSTED`, сообщение Google: “Your prepayment credits are depleted.” Это исчерпанный prepay баланс/платный план, а не просто дневной бесплатный лимит. Запрос списка моделей ранее был успешен, но не доказывал квоту генерации.
- `.env` приложения содержал `GEMINI_FREE_TIER=true`; это внутренний флаг учёта/UI и он не определяет реальный план Google. Из-за ответа Google исправлен на false, чтобы интерфейс и локальная оценка не называли prepay проект бесплатным. Это не меняет биллинг Google, не пополняет баланс и не отправляет дальнейших запросов.
- Официальная документация Google: при нулевом Prepay балансе Gemini API останавливается; вернуть проект на Free Tier можно отключением billing у проекта. Баланс и статус смотрятся в Google AI Studio → Billing, квоты — Dashboard → Usage. Не покупать кредит по умолчанию: минимум $5; пользователь предпочитает бесплатный вариант.
- Следующее действие владельца: открыть AI Studio Billing для проекта ключа и решить: 1) если хочет Free Tier — проверить возможность отключить billing проекта и его free-tier квоту; 2) если согласен на платный тест — самостоятельно рассмотреть Prepay, минимум $5 (кредиты ограничены по сроку/возврату). До выбора запросы не повторять.
- После первой команды restart api/worker внутри контейнера сохранился старый env (true). Пересозданы только api/worker через compose up --force-recreate; новый контейнер подтвердил GEMINI_FREE_TIER=false, ключ настроен (значение не выводилось), API health HTTP200. Генерация после диагностики не вызывалась.

## User correction: Gemini key was in OPENAI_API_KEY — 2026-09-19

- User clarified that the value entered as OPENAI_API_KEY is actually their Gemini key and explicitly requested not to use it for OpenAI or remove it.
- One preceding non-billable OpenAI GET /v1/models auth check had sent the configured OPENAI_API_KEY as Bearer; response was 401. The value is not recorded here. Told user to revoke/rotate the Gemini key because it was presented to api.openai.com.
- Cleared OPENAI_API_KEY in local .env without reading or displaying it; GEMINI_API_KEY left untouched. Recreate only API/worker, then verify booleans and health without transmitting key to OpenAI.
- Current Gemini recheck before this correction: new Gemini key GET /models HTTP200; one fictional CV probe succeeded and schema validation passed. App-side estimated actual cost $0.000221 under configured 3.5 Flash-Lite rates; this is an estimate, not a billing-console receipt. Probe ran against jobfinder_test.
- API/worker пересозданы. Проверка по точному контейнеру: Gemini configured=true, OpenAI configured=false, provider=gemini, free flag=false; API health HTTP200. Секреты повторно не передавались в OpenAI и не выводились. CODE snapshot/change state refreshed.

## 2026-09-20 — первый живой образец извлечения и проверка черновиков

- Продолжен выбранный пользователем пункт: несколько черновиков из первого сохранённого видео. Docker daemon был выключен; Docker Desktop запущен скрыто, compose поднят. Миграция завершилась успешно. Ключи не выводились и не менялись; OpenAI пуст, активен Gemini.
- Перед обработкой подтверждено: 0 extraction jobs, 0 вопросов, 2 сохранённые расшифровки, бюджет charged=$0.003429 при лимите $20. В основной базе 1 обычный пользователь, 0 администраторов.
- Через локальную служебную операцию поставлено задание штатному worker с владельцем — единственным существующим аккаунтом. Роль и API-права пользователя не менялись. Это авторизованная владельцем локальная подготовка общей базы, не проверка доступа через admin API.
- Source `3b16d41d-7640-4d69-b574-4efcfd604359` / video `zibAC8HkGFk`. Job `0e3c60cf-5106-47f8-b0c3-6e52518ea097`, request_key `source-zibAC8HkGFk-sample-271-574-v1`. В extraction-input записан ограниченный снимок технического фрагмента 271.52–580.68 с: 110 сегментов / 3472 символа. Исходная полная расшифровка не изменялась. При возобновлении использовать существующий job/checkpoints; не ставить этот образец повторно.
- Ровно 1 живой запрос Gemini: completed, 3 созданных draft questions. Резерв $0.020370, оценка фактической стоимости приложения $0.003683, месячный charged после запроса $0.007112. Это локальный расчёт по токенам, не подтверждённая сумма биллинга Google. Дополнительных генераций/embeddings/STT нет.
- При сверке выявлено: Gemini исправил спорный ответ кандидата про изменяемость, придумал оценочные interviewer_notes, оставил пустые roles при needs_context=false, неточно определил некоторые границы. Три карточки исправлены по субтитрам до версии 2, исходная версия сохранена в history. Аудио не прослушивалось; все needs_context=true, status=draft.
- Эталоны и критерии сверены с официальными docs.python.org (stdtypes, controlflow/default arguments, copy). Созданы 3 draft material с авторскими краткими изложениями и ссылками, привязаны к карточкам. Ничего не опубликовано и не индексировалось. Подробности и следующие шаги: docs/KNOWLEDGE_REVIEW.md, воспроизводимые данные проверки: docs/knowledge/first_sample.json.
- Изменения кода: tasks.extract_questions — явный запрет исправлять ошибки кандидата и выдумывать комментарии интервьюера; требование языка источника/отметки неоднозначной расшифровки; сервер принудительно ставит needs_context при пустых roles. Новый test_extraction_review.py проверяет сохранение ошибочного ответа, draft/needs_context, очистку непроверенных эталона/критериев/материалов и отсутствие точного дубля при повторной обработке. Фиксированный ответ, не доказательство качества новой инструкции на Gemini.
- Проверка: targeted pytest на jobfinder_test — **1 passed**, 2 прежних deprecation warnings. Пересобраны/перезапущены API/worker; сборка exit0. Проверка основной БД: 3 draft версии 2 с history и draft materials, published=0; job completed, 1 Usage; новая проверка ролей установлена; API health HTTP200. Тестовые фикстуры основную БД не затрагивали.
- Техническая оговорка: применение review JSON успешно закоммитилось; удаление временного файла docker cp пользователем worker затем дало PermissionError. Пересоздание контейнера удалило временный файл, отсутствие подтверждено. Повторное применение данных не требовалось.
- Следующее: аудиосверка спорных реплик, настройка административного доступа без автоматического повышения обычного аккаунта, проверка/публикация материалов и карточек, затем оставшиеся фрагменты. Вся база из 90 вопросов и живые сценарии CV/ранжирования/оценки RU/EN пока не завершены. Новая инструкция ещё не проверена повторной живой генерацией.
- Снимок кода обновлён: 59 файлов, 2 изменённые полные версии записаны в CODE_CHANGES (tasks.py и новый тест); CODE_SNAPSHOT актуален. JSON проверки и журналы не содержат ключей или пользовательских документов.

## 2026-09-20 — административный доступ и рабочее место проверки

- Пользователь поручил выполнить следующий шаг: закончить первые карточки и административный доступ. Проверено наличие единственного локального аккаунта; по этому поручению его роль изменена user → admin. ADMIN_EMAIL в локальном .env приведён к адресу этого аккаунта, адрес/секреты не выводились. Это явная разовая настройка по поручению владельца; код регистрации/проверки прав не ослаблялся, пароль не менялся.
- Административный доступ проверен через настоящий HTTP API с временной сессией на 5 минут и CSRF/Origin. GET /admin/question HTTP200. Временная сессия удалена в finally, существующие сессии не менялись; пользователю достаточно обновить страницу.
- Три подготовленных reviewer_summary материала docs.python.org опубликованы через POST /admin/publish/{id}. Для каждого из трёх вопросов тот же API вернул ожидаемый HTTP422 из-за needs_context=true. Вопросы не опубликованы. Дополнительных вызовов Gemini/OpenAI не выполнялось.
- В этой сессии нет инструмента прослушивания аудио; автоматическую расшифровку не выдаём за независимую аудиосверку. Спорные реплики всё ещё требуют проверки владельцем/редактором. Сделан конкретный интерфейс для этого шага, см. KNOWLEDGE_REVIEW.md.
- Добавлен SourceReview.tsx: замечания/границы предыдущей проверки, ссылки на видео, раскрываемый фрагмент субтитров только пересекающий интервал карточки, ссылки на каждый таймкод, сообщение об отсутствии субтитров. Для единственного источника интервал обновляется при редактировании start/end. Невалидный video ID не превращается в ссылку; внешний проигрыватель автоматически не загружается.
- main.tsx подключает SourceReview в редактор; у start/end добавлен step=any — значения с дробными секундами раньше могли блокировать submit браузерной валидацией. typography.css задаёт оформление блока. README обновлён.
- Административный Playwright расширен проверкой ссылки, текста субтитров и сохранения 1.25/15.75 с; дальше проверяет отказ неполной публикации и успешную публикацию исправленной тестовой карточки. **1 passed (12.9 с)** на отдельном jobfinder_test. Внешние ответы фиксированные. Frontend TypeScript/Vite build успешен. E2E-контейнер остановлен и удалён.
- В tests/e2e_server.py явно закреплён синтетический ADMIN_EMAIL=owner@example.com, чтобы тесты не зависели от настоящего адреса владельца в .env. Успешный запуск выше имел то же значение через override -e; код фикстуры закрепляет этот override для следующих сессий.
- Запущена локальная пересборка web/api/worker для установки нового интерфейса и применения ADMIN_EMAIL. После неё проверить HTTP200, совпадение ADMIN_EMAIL с единственным admin, 3 published materials / 3 draft questions, неизменный бюджет и обновить snapshots.
- Финал установки: compose build/up exit0; API health HTTP200, web HTTP200 отдаёт index-DqQ1uZod.js. В main подтверждены ADMIN_EMAIL matches admin=true, 3 published materials, 3 draft questions с needs_context, charged=$0.007112 (не изменился), OpenAI configured=false. Snapshot: 60 файлов, 6 полных изменённых версий записаны; пользовательские адреса/ключи исключены. Остаётся аудиосверка пользователем через подготовленный экран; её отсутствие — реальное ограничение инструментов этой сессии, не требование дополнительного разрешения.

## 2026-09-20 — второй образец и первый опубликованный вопрос

- По поручению продолжить наполнение выбрано второе сохранённое видео UYmA6p7UwOo / source 7cc5f50f-559e-4a2f-a1f0-e4bc9c6195ac. У первого три карточки всё ещё требуют аудиосверки; повторных вызовов для них нет. Администратор и ключи не менялись.
- Job ad291af1-5102-4e9e-a24e-636bd0c15a3d, request_key source-UYmA6p7UwOo-sample-255-590-v1. В checkpoint сохранены 138 сегментов / 4199 символов, 255.519–593.48 с; рекламная часть после обсуждения не передавалась, кроме завершающего перехода в последнем сегменте. Worker: completed, 4 новых черновика. Ровно 1 живой Gemini вызов, оценка actual=$0.005136, месячный charged=$0.012248. Не равно выписке Google; OpenAI/STT/embeddings не вызывались.
- Живая проверка новой инструкции показала: ошибки кандидата сохранены, но модель опустила исправления интервьюера и обрезала таймкоды. Исправлены вручную по тексту, никакого утверждения об аудиопрослушивании. Данные редакторской сверки: docs/knowledge/second_sample.json.
- Два вопроса хэширования (93587504-6dcf-445d-8ca8-408b1770edbf и c740476a-18ef-4b4e-9756-4e1f82ff20fb) объединены через admin API, второй status=merged. Сохранены merged_discussions и обе исходные ссылки. Объединённый вопрос и сложность d02c8225-75ff-4723-b684-eed5d60c8e74 оставлены draft/needs_context=true с конкретными замечаниями.
- Декораторы d8dde20e-a402-4c5f-ac07-02b800c87ce4 проверены по всему обсуждению 477.0–590.44 и официальной документации compound_stmts/function-definitions. Роли подтверждаются последовательными репликами, экранного условия нет. Эталон различает применение декоратора при выполнении определения и вызов возвращённой обёртки. Существенной неоднозначности текста не найдено; needs_context=false и вопрос опубликован через API. Это текстовая редакторская проверка, не аудиопроверка.
- Созданы и опубликованы 3 reviewer_summary материала: Python datamodel/hash, bisect/performance (дополнительно архивная Python Wiki TimeComplexity), compound_stmts/decorators. Предыдущие 3 материала сохранены. Прямые изменения карточек сохраняют history и повышают version, исходные ответы модели остаются в checkpoint/history. Для HTTP merge/publish использована временная сессия с CSRF/Origin, затем удалена; временный JSON удалён из /tmp точным путём.
- Итоговые проверки основной базы (без тестовых фикстур): 6 published materials, 1 published question, 5 draft questions, 1 merged question. Pydantic карточки, минимум критериев, материалы/Knowledge, сохранённая история объединения, completed job и один completed Usage подтверждены. Штатные retrieve/evidence находят опубликованный вопрос с фильтрами python/junior/ru. Код приложения не менялся, сборка/pytest не повторялись.
- Следующее: до полноценного интервью нужно ещё 4 опубликованных вопроса. Первый образец остаётся на аудиосверке, из второго неясны хэширование и обозначения сложности. Для дальнейшего извлечения второго видео брать техническую часть после 671.76 с, пропустив рекламу; полноценных импортов двух записей ещё нет. Не повторять два completed образца: использовать их checkpoints/историю. Живую оценку RU/EN ещё предстоит проверить после наполнения.

## 2026-09-20 — пять опубликованных вопросов для первой сессии

- Прочитан следующий сохранённый фрагмент UYmA6p7UwOo (671.76–1230 с). Для подготовки выбраны четыре явно заданных вопроса: стандартная async-обёртка, lambda, контекстный менеджер, finally. Получение/распознавание видео повторно не запускалось.
- Карточки подготовлены редактором напрямую из субтитров: docs/knowledge/third_sample.json. Внешний Gemini не вызывался. Кандидатские ошибки сохранены в отдельных кратких изложениях; ответы интервьюера не приравниваются к эталонам. Аудио не прослушивалось. Вопрос о типе корутины 752.64–811.92 пропущен из-за неразборчивого имени метода, реклама исключена. Новые четыре обсуждения имеют достаточно ясный технический смысл и распределение ролей по репликам для текстовой проверки.
- Эталоны/критерии сверены с официальными Python asyncio-task, expressions/lambdas, compound_stmts/with и function-definitions, contextlib, tutorial/errors/cleanup. Уточнены ограничения утверждений: обычная схема декоратора без автоматического await; lambda содержит выражение; __exit__ после успешного __enter__; finally не гарантируется при принудительном завершении процесса.
- Созданы 4 общие карточки с Pydantic-валидацией, точными source_id/video_id/start/end, review_batch=third-manual-review-2026-09-20, review_scope и review_note; по одной самостоятельной карточке на явный вопрос, finally отдельно от протокола with. Дедупликация использует source_id + нормализованный вопрос, повтор применения batch проверяет существующие записи. Созданы 4 reviewer_summary материала. Все 8 объектов опубликованы через настоящий admin HTTP API с временной сессией/CSRF/Origin; временная сессия и JSON в /tmp удалены.
- Новые question IDs: async-wrapper f2dd893d-8dde-42c9-850c-28e1411fe7f5; lambda 38e46f2a-6154-4682-8f09-62c829c20c6c; context-manager 981f9c41-b5fc-4591-8123-ef06f2b5ca34; finally 62aef940-bcad-4970-8e1c-f144e58deff8.
- Реальная проверка main: retrieve('Python', python, junior, ru, 100) даёт 5 различных опубликованных вопросов, у всех валидная схема, needs_context=false, >=3 критериев, положительный интервал источника и опубликованное evidence. Состояния: 5 published questions / 5 draft / 1 merged; 10 published materials. Зарезервировано/учтено по-прежнему $0.012248 — новых расходов нет.
- Важное ограничение запуска: HH_ACCESS_TOKEN отсутствует, вакансий в основной БД 0. База вопросов теперь достаточна для пяти ходов, но полный пользовательский запуск требует вакансии. В личный аккаунт синтетические вакансии/интервью не добавлялись; пользователь по требованию не должен создавать вакансии вручную.
- Код приложения не изменялся, повтор сборок/фикстур не нужен. Следующий независимый шаг — синтетическое живое интервью/оценка Gemini в jobfinder_test (RU, затем EN отдельно с подходящей базой), фиксация качества правильных/частичных/неверных ответов и расходов. Затем настройка HH владельцем и полный сценарий. Нельзя считать проверку retrieve проверкой живой оценки или законченной пользовательской сессии.

## 2026-09-25 — Git и перенос устройства

- Пользователь поручил push и перенос контекста/данных. До этого .git отсутствовал: CODE_CHANGES/CODE_SNAPSHOT были снимками, не коммитами. Инициализирован main, используется существующая Git identity. Из-за разных Windows-владельцев применяется -c safe.directory только для этой папки/команды, глобальная настройка не менялась.
- URL remote запрошен и пока не получен. Push без него невозможен; чужой или публичный remote не выбирался. Первый commit фиксирует текущее состояние, не вымышленную прошлую историю.
- Добавлены SESSION_HANDOFF.md и DEVICE_TRANSFER.md, скрипт backup-transfer.ps1. Это доступный рабочий контекст, не полный дословный экспорт чатов/внутренних сессий. Полные чаты пользователь отдельно экспортирует из приложения при необходимости.
- Docker Desktop запущен скрыто. Создан backups/transfer-20260925-190720: pg_dump основной базы, storage, .env, инструкция, SHA256. API/worker остановлены на время согласованной копии и затем возвращены в исходное состояние. Тестовая БД не копировалась.
- pg_restore --list успешен, SHA256 всех 7 файлов проверены. Полное восстановление на другом устройстве ещё не проверялось. Приватная копия не зашифрована и содержит секреты/личные данные: не пушить. .gitignore дополнен backups/dump/bundle и локальными каталогами агента.
- Подготовленные 74 файла проверены на значения настроенных API secrets и распространённые сигнатуры токенов: совпадений нет. .env/backups/storage не staged. Ключи не выводились. Это целевая проверка, не универсальный аудит персональных данных.
- Следующее: первый commit, bundle/source archive и приватный tar.gz; push после получения URL. Платные API в этом checkpoint не вызывались.
- Первый commit создан: 5942cb8 (Initial JobFinderKZ prototype with durable context and device transfer guide), 74 файла. Working tree после него чистое. Подготовлены backups/project.bundle (git bundle verify успешен), backups/source.zip, backups/private-data-20260925.tar.gz; tar inventory содержит .env и jobfinder.dump. Контрольные суммы архивов — backups/ARCHIVE_SHA256.json. API/db healthy, web/worker запущены. После записи этого checkpoint bundle/source и их хэши обновляются на финальный commit документации. Remote/push ещё не выполнен: URL пользователя отсутствует.

## 2026-09-25 — push в GitHub завершён

- Пользователь предоставил https://github.com/NUKI1223/jobfinderkz.git и разрешил push. ls-remote подтвердил отсутствие refs, remote origin добавлен; main отправлен обычным push без force, tracking origin/main настроен. Код/журналы/инструкции переноса в GitHub; .env и backups не отслеживаются.
- После записи результата этот checkpoint также коммитится и отправляется; bundle/source.zip и SHA256 обновляются. Приватный архив данных остаётся только локально. На новом устройстве можно git clone указанного URL и восстановить приватную копию по DEVICE_TRANSFER.md. Видимость репозитория агентом не менялась.
